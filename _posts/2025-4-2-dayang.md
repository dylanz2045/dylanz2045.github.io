---
title: 本地docker部署大洋前后端项目
categories: [docker，nodejs,golang]
tags: [docker，nodejs,golang]
author: ailanz
pin: true
description: 本文章是全称记录外包大洋项目时，本地部署运行环境时出现的问题及解决办法 可供后续项目部署参考 # 文章简要描述
comments: true # 默认开启文章评论，若要关闭则填写false
---

## 教培本地运行大洋后端项目

### 背景 

在windows主机的情况下，编译运行基于Linux运行环境下的golang项目，并且使用穿透脚本将数据进行转发到远程服务端。

#### 使用工具/思路

- ubuntu:latest镜像创建服务端镜像
- windows主机创建穿透脚本容器
- network网络通讯

#### 完整实现思路过程

- 首先，在本地拉取这个项目，就去看他的README文件，并且首次编译，就出现很多Unix的包，还有一些自定义仓库的包，就觉得不能。并且需要我们使用的脚本，都是使用sh脚本的，因此觉得不能在windows中运行。
<img src=/assets/img/2025-4-2-dayang.assets/image-20250404204549602.png >


- 就开始有点迷茫，随后尝试去问GPT，看看有什么办法去在windows中运行sh脚本，冒出来是使用git bash的方法，还有各种各样的方法，觉得没有一点头绪。

- 然后突然就想到当时我们的坦克后端，也同样是运行在linux容器中的，也就有个想法，创建一个ubuntu:latest镜像的容器，让我们的项目运行在上面。就开始干；因为容器之间的通讯其实很简单，因此是印证了能实现容器内部跟我宿主机的容器进行通讯的，所以就更加坚定我的想法。

- 之后就先在windows下，将这个sh脚本，转化为bat脚本，同样是可以创建容器的。然后就开始创建容器，一开始发现我创建的容器，无法运行，实际上挂载的命令是没错的，就是这个`-it`的参数，能让我的容器保持一个持续运行的状态。然后想使用`-v`这个参数，将我主机的项目挂载进到容器中，想实现这个直接编译成可执行文件。

- 在挂载之后，执行`go build`的时候，发现此时很多依赖包是需要仓库的授权，或者说是要从私有仓库中拉取的，因此就需要使用`git`，在容器中去下载这个`git`的版本，然后从仓库中进行拉取。在拉取的时候，发现我在输入了`username`跟`password`之后，还是不能成功拉取，是因为在Linux环境下，没有交互式的界面让`git`去验证我们的权限。因此需要使用到`/etc/netrc`文件，自动的将这个账号密码都放置到配置文件中，能自动传输到`git`中，就能解决这个无法拉取的问题

- 随后能成功拉取了，也成功`build`。之后就尝试去运行`run.sh`脚本，但是发现里面无法执行，有个配置文件`skillexam_base.properties`无法找到，经过询问之后，是他们没有做好这个README的更新，遗漏更新这个`run.sh`脚本的文件路径，随后读取了新的配置文件之后，报错就消失了
<img src=/assets/img/2025-4-2-dayang.assets/image-20250404210531018.png >

- 随后成功看到项目启动，但是看到这个IP，找不到地址
<img src=/assets/img/2025-4-2-dayang.assets/image-20250404210643036.png >

- 在修改了这个为本地的模式，修改`/etc/hosts`的配置文件：为`127.0.0.1`，但是意识到这个IP肯定是无法访问的。因为在容器的内部，与宿主机是互相隔离的。因此就尝试了`docker.internal.network`这个属性，发现也不行，无法执行。原因是`/etc/hosts`这个优先级会比docker的优先级高，会首先读取DNS的命令，不会自动读取到docker的任何指令。

- 第一个想法是：在容器内部，再次创建新的容器，这样他们就能同属一个网络了，就可以使用127.0.0.1了，但是在实现的过程中，一直无法挂载成功docker.sock的套接字，也就是子容器无法守护docker进程，也就无法使用docker指令，一直找不到方法解决

- 再之后就突然想起，既然我是想解决容器的网络不同的问题，那刚好容器的network就能解决这个容器之间的通讯，我创建一个新的网路，然后将我服务端添加到这个网络中，再将这个穿透脚本的容器，也添加到这个skillexam网络中，之后通过IP地址进行通讯。在`/etc/hosts`添加上127.20.0.2这个IP，就能成功找到同一个IP的不同端口的容器。

- 之后就全部都通了；中间经历了一些小插曲，就是直接发送https的网路请求，直接无法经过ssl证书的保证。

- 至此就成功运行服务端项目

#### 总结

- 本次非常考验自己在现有的知识上，对于完全不懂得的东西，去学会掌握，运用他。对此，我对整个容器的通讯，还有Linux、windows操作系统又多进了一步。



#### 具体实现如下



## 教培本地部署服务端

#### 补充使用dockerFile的形式，就不需要每一次都要进入到容器内部进行重新执行复杂指令了。

- 进入windows下拉取的项目路径，新增文件transport.bat，然后打开cmd执行

  ```shell
  .\transport.bat
  ```

- 使用指令创建Linux环境的服务端容器

  ```shell
  docker run -it --name=skillexam -p 5739:5739 ubuntu:latest 
  ```

- 之后在windows主机的终端中

  ```shell
  docker network create skillwork
  
  docker network connect skillexam skillwork
  
  docker network connect 穿透脚本创建的容器名 skillwork
  
  # 查看这个网络下，穿透脚本创建的容器的IP，记住这个IP
  docker inspect network skillwork
  ```

- 执行指令进去这个容器内部终端

  ```shell
  docker exec -it skillexam bash
  ```

- 在容器内部依次执行

  ```sh
  apt update
  ```

  ```shell
  apt-get install uchardet libuchardet-dev
  ```

  ```shell
  apt-get install -y libiconv
  
  wget http://ftp.gnu.org/pub/gnu/libiconv/libiconv-1.16.tar.gz
  # 解压
  tar -zxvf libiconv-1.16.tar.gz
  # 进入目录
  cd libiconv-1.16
  # 配置
  ./configure --prefix=/usr/local/libiconv
  # 编译
  make
  # 安装
  make install
  ```

  ```shell
  apt-get install -y nano
  nano ~/.netrc
  
  # 输入 替换成自己的git 的账号与密码
  machine w.gdy.io
  login username
  password password
  
  
  apt install git -y
  # 进入到自己想要放置到的目录
  git clone -b v7.0.0 https://w.gdy.io/dyf/skillexam-srv.git
  ```

  ```bash
  # 执行一下，重载容器配置
  source /etc/profile
  ```

  

  ```shell
  # 这个指令我不确定
  apt install golang 
  
  go env -w GOPROXY=https://goproxy.cn,direct
  go env -w GOPRIVATE="w.gdy.io"
  
  go build 
  go mod tidy
  ```

  - 创建一个文件夹后，执行

    ```shell
    touch skillexam-base.properties
    
    nano skillexam-base.properties
    
    # 复制下面代码到文件中
    
    
    
    [default]
    SKILL_EXAM_MODE=manager
    FORWARD_PREFIX=skillnode_
    COMMON_PUB_PROTO=https:
    CLOUD_NODE_MAIN_HOST=dev.gdy.io
    LOG_SKILL_EXAM_ADDR=/var/log/skillexam/
    LOG_REDIRECT=1
    DEV_MODE=
    
    # 电子商务师注册页面
    E_COMMERCE_REGIST_PAGE=/student_ANCHOR_/cp/exam-register/detail
    UPLOAD_REDIRECT=0
    FS_PUB_HOST=fs.v370.rcp.gdy.io
    FS_INNER_ADDR=fs.loc:2010
    FS_RC_CON=fs.loc:2011
    FS_RC_SIZE=10240000
    FS_UPLOADURL=http://fs.loc:2010/in/api/uload
    FS_DLOADURL=http://fs.loc:2010/in/api/dload?fid=%v
    
    UCS_RC_CON=ucs.loc:1842
    UCS_RC_SIZE=10240000
    UCS_PUB_HOST=sso.v370.rcp.gdy.io
    SSO_LOGIN_URL=${default/COMMON_PUB_PROTO}//${default/UCS_PUB_HOST}/sso/index.html?url=%v
    SSO_AUTH_API=${default/COMMON_PUB_PROTO}//${default/UCS_PUB_HOST}/sso/api/auth?token=%v
    
    TMS_INNER_HOST=tms.loc:1973
    TMS_RC_SIZE=10240000
    TMS_PUB_HOST=tms.v200.skilltms.gdy.io
    
    COURSE_PUB_HOST=course.v370.rcp.gdy.io
    COURSE_RC_SIZE=10240000
    COURSE_RC_CON=course.loc:1919
    
    SKILL_EXAM_REDIS_CON=redis.loc:6379
    SKILL_EXAM_DB_CON=mongodb://cny:123@mongo.loc:27017/dyf_skill?maxPoolSize=8&authMechanism=SCRAM-SHA-1&authSource=admin
    
    SKILL_EXAM_LISTENR=:5739
    SKILL_EXAM_RC_ADDR=:5740
    SKILL_EXAM_RC_SIZE=10240000
    
    TO_PDF_URL=http://render.loc:7543
    SKILL_EXAM_PUB_HOST=skill.v300.skillproxy.gdy.io
    SKILL_EXAM_MANAGER_ADDR=https://skill.v300.skillproxy.gdy.io
    SKILL_SERVICE_PUB_HOST=skillservice.v300.skillproxy.gdy.io
    SKILL_EXAM_EXTRA_TOKEN=6f3f9154962f982b4a089d490b2a368e84310294e284b390af9c1a57aa503c9c
    
    INIT_DATA_PATH=initdata/
    SKILL_EXAM_API_FILE=${default/INIT_DATA_PATH}/api.json
    
    DEFAULT_AVATAR=conf/default-avatar.png
    PAPER_RENDER_TEMPLATE=conf/exam-info-index.html
    SKILL_EXAM_OUTPUT_PATH=/tmp/
    NOT_FOUND_PAGE=/app/skillexam-srv/www/skill_exam_web/not-found
    WEB_DIR=/app/skillexam-srv/www
    
    ENABLE_PAY=0
    MOCK_PAY=0
    ALIPAY_CONFIG=conf/alipay.conf
    WCPAY_CONFIG=conf/wxpay.conf
    
    # 轮询时间
    CREATE_TEST_POLLING_INTERVAL=30000000000
    # 提前生成时间 5*60*1000000000
    CREATE_TEST_BEFORE_TIME=300000000000
    
    INDEX_REDIRECT=${default/COMMON_PUB_PROTO}//${default/UCS_PUB_HOST}/skill-login
    
    RSC_ENABLE=0
    RSC_CALLER_NAME=
    RSC_MASTER=
    RSC_CALLER_INDEX=
    RSC_MASTER_ADDR=
    RSC_ACCESS_TOKEN=
    
    # 阿里云短信
    USE_ALIYUN_SMS=1
    ALY_ACCESS_ID=LTAI5tKCkAQeeU8ZwKxEqypB
    ALY_ACCESS_KEY=AUq2labrYQCgZHZdt9B9dLf9IxDKKI
    # 短信签名
    ALY_SMS_SIGN_NAME=大洋教育
    
    # 天翼云短信
    USE_CTYUN_SMS=1
    CTYUN_ENTERPRISE_NO=GD10A643977
    CTYUN_ACCOUNT=02012564615
    CTYUN_SECRETKEY=5A9722FC7536D94BF5F7670D2EACECC2
    
    # 除了modePriority，其他数据结构和酷校项目一致
    # vue项目调用sdk传配置时，需要前端把下面配置转成和中台项目一致
    UPLOAD_CONFIG={ "imageLimit": { "default": { "height": 3840, "width": 3840 } }, "sizeLimit": { "default": { "default": 52428800 } }, "ossConfig": { "default": { "ext": ["3gp", "avi", "flv", "mp4", "m3u8", "mpg", "asf", "wmv", "mkv", "mov", "ts", "webm"] } }, "ossType": "ALY", "modePriority": { "default": { "default": [ "FS" ] } } }
    
    PROMETHEUS_OPEN=1
    PROMETHEUS_WEB_UI_ADDR=
    
    [fs]
    
    FS_DIR=${ENV_FS_DIR}
    FS_DB_CON=${ENV_FS_DB_CON}
    FS_PUB_HOST=${ENV_FS_PUB_HOST,default/FS_PUB_HOST}
    FS_INNER_ADDR=${ENV_FS_INNER_ADDR,default/FS_INNER_ADDR}
    FS_RC_CON=${ENV_FS_RC_CON,default/FS_RC_CON}
    FS_RC_SIZE=${ENV_FS_RC_SIZE,default/FS_RC_SIZE}
    FS_UPLOADURL=${ENV_FS_UPLOADURL,default/FS_UPLOADURL}
    FS_DLOADURL=${ENV_FS_DLOADURL,default/FS_DLOADURL}
    
    [ucs]
    
    UCS_DB_CON=${ENV_UCS_DB_CON}
    UCS_RC_CON=${ENV_UCS_RC_CON,default/UCS_RC_CON}
    UCS_RC_SIZE=${ENV_UCS_RC_SIZE,default/UCS_RC_SIZE}
    UCS_PUB_HOST=${ENV_UCS_PUB_HOST,default/UCS_PUB_HOST}
    SSO_LOGIN_URL=${ENV_SSO_LOGIN_URL,default/SSO_LOGIN_URL}
    SSO_AUTH_API=${ENV_SSO_AUTH_API,default/SSO_AUTH_API}
    
    [tms]
    TMS_INNER_HOST=${ENV_TMS_INNER_HOST,default/TMS_INNER_HOST}
    TMS_RC_SIZE=${ENV_TMS_RC_SIZE,default/TMS_RC_SIZE}
    TMS_PUB_HOST=${ENV_TMS_PUB_HOST,default/TMS_PUB_HOST}
    
    [course]
    COURSE_PUB_HOST=${ENV_COURSE_PUB_HOST,default/COURSE_PUB_HOST}
    COURSE_RC_SIZE=${ENV_COURSE_RC_SIZE,default/COURSE_RC_SIZE}
    COURSE_RC_CON=${ENV_COURSE_RC_CON,default/COURSE_RC_CON}
    
    [skillexam]
    LOG_REDIRECT=${ENV_LOG_REDIRECT,default/LOG_REDIRECT}
    
    UPLOAD_REDIRECT=${ENV_UPLOAD_REDIRECT,default/UPLOAD_REDIRECT}
    LOG_SKILL_EXAM_ADDR=${ENV_LOG_SKILL_EXAM_ADDR,default/LOG_SKILL_EXAM_ADDR}
    
    SKILL_EXAM_MODE=${ENV_SKILL_EXAM_MODE,default/SKILL_EXAM_MODE}
    
    FORWARD_PREFIX=${ENV_FORWARD_PREFIX,default/FORWARD_PREFIX}
    COMMON_PUB_PROTO=${ENV_PUB_PROTO,default/COMMON_PUB_PROTO}
    
    CLOUD_NODE_MAIN_HOST=${ENV_CLOUD_NODE_MAIN_HOST,default/CLOUD_NODE_MAIN_HOST}
    
    SKILL_EXAM_REDIS_CON=${ENV_SKILL_EXAM_REDIS_CON,default/SKILL_EXAM_REDIS_CON}
    SKILL_EXAM_DB_CON=${ENV_SKILL_EXAM_DB_CON,default/SKILL_EXAM_DB_CON}
    
    SKILL_EXAM_LISTENR=${ENV_SKILL_EXAM_LISTENR,default/SKILL_EXAM_LISTENR}
    SKILL_EXAM_RC_ADDR=${ENV_SKILL_EXAM_RC_ADDR,default/SKILL_EXAM_RC_ADDR}
    SKILL_EXAM_RC_SIZE=${ENV_SKILL_EXAM_RC_SIZE,default/SKILL_EXAM_RC_SIZE}
    
    ENABLE_PAY=${ENV_ENABLE_PAY,default/ENABLE_PAY}
    MOCK_PAY=${ENV_MOCK_PAY,default/MOCK_PAY}
    ALIPAY_FILE=${ENV_ALIPAY_FILE,default/ALIPAY_FILE}
    
    TO_PDF_URL=${ENV_TO_PDF_URL,default/TO_PDF_URL}
    SKILL_EXAM_PUB_HOST=${ENV_SKILL_EXAM_PUB_HOST,default/SKILL_EXAM_PUB_HOST}
    SKILL_EXAM_MANAGER_ADDR=${ENV_SKILL_EXAM_MANAGER_ADDR,default/SKILL_EXAM_MANAGER_ADDR}
    SKILL_SERVICE_PUB_HOST=${ENV_SKILL_SERVICE_PUB_HOST,default/SKILL_SERVICE_PUB_HOST}
    SKILL_EXAM_EXTRA_TOKEN=${ENV_SKILL_EXAM_EXTRA_TOKEN,default/SKILL_EXAM_EXTRA_TOKEN}
    SKILL_EXAM_VENUE_ID=${ENV_SKILL_EXAM_VENUE_ID}
    
    INIT_DATA_PATH=${ENV_INIT_DATA_PATH,default/INIT_DATA_PATH}
    SKILL_EXAM_API_FILE=${ENV_SKILL_EXAM_API_FILE,default/SKILL_EXAM_API_FILE}
    
    DEFAULT_AVATAR=${default/DEFAULT_AVATAR}
    PAPER_RENDER_TEMPLATE=${default/PAPER_RENDER_TEMPLATE}
    SKILL_EXAM_OUTPUT_PATH=${default/SKILL_EXAM_OUTPUT_PATH}
    NOT_FOUND_PAGE=${default/NOT_FOUND_PAGE}
    WEB_DIR=${default/WEB_DIR}
    E_COMMERCE_REGIST_PAGE=${default/E_COMMERCE_REGIST_PAGE}
    
    # 轮询时间
    CREATE_TEST_POLLING_INTERVAL=${default/CREATE_TEST_POLLING_INTERVAL}
    # 提前生成时间 5*60*1000000000
    CREATE_TEST_BEFORE_TIME=${default/CREATE_TEST_BEFORE_TIME}
    
    INDEX_REDIRECT=${ENV_INDEX_REDIRECT,default/INDEX_REDIRECT}
    DEV_MODE=${ENV_DEV_MODE,default/DEV_MODE}
    
    RSC_ENABLE=${ENV_RSC_ENABLE,default/RSC_ENABLE}
    RSC_CALLER_NAME=${ENV_RSC_CALLER_NAME,default/RSC_CALLER_NAME}
    RSC_MASTER=${ENV_RSC_MASTER,default/RSC_MASTER}
    RSC_CALLER_INDEX=${ENV_RSC_CALLER_INDEX,default/RSC_CALLER_INDEX}
    RSC_MASTER_ADDR=${ENV_RSC_MASTER_ADDR,default/RSC_MASTER_ADDR}
    RSC_ACCESS_TOKEN=${ENV_RSC_ACCESS_TOKEN,default/RSC_ACCESS_TOKEN}
    
    # 阿里云短信
    USE_ALIYUN_SMS=${ENV_USE_ALIYUN_SMS,default/USE_ALIYUN_SMS}
    ALY_ACCESS_ID=${ENV_ACCESS_ID,default/ALY_ACCESS_ID}
    ALY_ACCESS_KEY=${ENV_ACCESS_KEY,default/ALY_ACCESS_KEY}
    # 短信签名
    ALY_SMS_SIGN_NAME=${ENV_ALY_SMS_SIGN_NAME,default/ALY_SMS_SIGN_NAME}
    
    # 天翼云短信
    USE_CTYUN_SMS=${ENV_USE_CTYUN_SMS,default/USE_CTYUN_SMS}
    CTYUN_ENTERPRISE_NO=${ENV_CTYUN_ENTERPRISE_NO,default/CTYUN_ENTERPRISE_NO}
    CTYUN_ACCOUNT=${ENV_CTYUN_ACCOUNT,default/CTYUN_ACCOUNT}
    CTYUN_SECRETKEY=${ENV_CTYUN_SECRETKEY,default/CTYUN_SECRETKEY}
    
    # 文件上传
    UPLOAD_CONFIG=${ENV_UPLOAD_CONFIG,default/UPLOAD_CONFIG}
    
    #支付配置
    ALIPAY_CONFIG=${ENV_ALIPAY_CONFIG,default/ALIPAY_CONFIG}
    WCPAY_CONFIG=${ENV_WCPAY_CONFIG,default/WCPAY_CONFIG}
    
    PROMETHEUS_OPEN=${ENV_PROMETHEUS_OPEN,default/PROMETHEUS_OPEN}
    PROMETHEUS_WEB_UI_ADDR=${ENV_PROMETHEUS_WEB_UI_ADDR,default/PROMETHEUS_WEB_UI_ADDR}
    ```

  - 之后回到项目路径 修改`run.sh`文件中的`N_RCP_ALL_CFG`为：

    ```shell
    export N_RCP_ALL_CFG=/你刚刚创建的文件夹路径/skillexam_base.properties
    ```

  ```shell
  nano /etc/hosts
  
  #在最后一行添加
  之前查看穿透容器的IP mongo.loc redis.loc fs.loc ucs.loc course.loc
  ```

  ```shell
  ./run.sh
  ```

- 成功运行，登录网站`https://v650.skillproxy.gdy.io/` 账号：`c2` 密码：`123456`

- 登录成功之后，打开浏览器开发者工具，清空所有日志，之后点击`我的证书`栏，看到网络有个`listMyCertificate`的请求，打开查看`常规` ，将这个请求url拿到，然后查看`请求标头`的cookie，将`token=??`全部复制，使用自己喜欢的测试工具，发送请求即可。

#### 注意事项：

- 操作容器内文件，可以利用vscode的插件：`Dev containers`，这个插件能直接连接到容器，直接使用vscode去编辑容器内的文件

由于每一次停止这个穿透脚本，就会删除掉这个穿透容器，因此每一次创建这个穿透容器，就要执行一次

```shell
docker network connect 穿透脚本创建容器名 skillwork
```

- 但是不需要重新修改Linux容器中的`/etc/hosts`的IP地址，因为这个不会根据容器的清除而改变





## 教培部署前端运行环境

- 安装对应版本的nodejs版本  去node官网查找对应的tag

  - ```http
    https://nodejs.org/en/blog/release/v16.15.0
    ```

- 之后直接找到对应的tag的连接，使用

  ```bash
  wget https://nodejs.org/dist/v16.15.0/node-v16.15.0-linux-x64.tar.xz
  ```

  去下载这个包 直接二进制

